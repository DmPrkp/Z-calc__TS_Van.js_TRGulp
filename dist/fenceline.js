/*const computeForm = document.forms['computeForm'];
const inputMetr = computeForm.elements.inputMetr;
let arrLineProp = [];

computeForm.addEventListener('submit', function (e) {
    e.preventDefault();
    let controlElement1 = this.elements.firstPoints1;    

    for (let i = 0; i < controlElement1.length; i++) {
        if (controlElement1[i].selected) {
            arrLineProp.push(controlElement1[i].value); 
        }
    }

    arrLineProp.push.inputMetr.value;

    let controlElement2 = this.elements.firstPoints2;    

    for (let i = 0; i < controlElement2.length; i++) {
        if (controlElement2[i].selected) {
            arrLineProp.push(controlElement2[i].value); 
        }
    }

    console.log(arrLineProp)        
});*/
/*
let arr1 = ["edgePillar", 32, "coner"];
let arr2 = ["coner", 18, "edge"];

let obj = {arr1, arr2};

console.log(obj)*/
